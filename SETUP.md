# Setup Instructions for LuiseStore Fork

After forking this repository, you need to update several placeholder URLs to point to your own repository.

## Files to Update

All repository URLs have been configured for GitHub username: **xztimee**

### Updated Files:

1. **GitHub Issue Template** - `.github/ISSUE_TEMPLATE/bug-report-issue-template.yml`
   - Issue tracker: `https://github.com/xztimee/LuiseStore/issues`

2. **Workflow Documentation** - `.github/workflows/README.md`
   - Repository: `https://github.com/xztimee/LuiseStore`

3. **Build Workflow** - `.github/workflows/build-release.yml`
   - Documentation link: `https://github.com/xztimee/LuiseStore`

### 3. Build Workflow (if needed)
**File:** `.github/workflows/build-release.yml`
- Update any repository-specific URLs or references

## Repository Configuration

All URLs have been configured to use: `https://github.com/xztimee/LuiseStore`

No further URL updates are needed unless you want to change the repository location.

## Verification

After updating, verify the changes by:
1. Checking that all URLs point to your repository
2. Testing the issue tracker link
3. Ensuring documentation links work correctly

## Optional: Update Repository Description

Don't forget to update your GitHub repository description and topics to reflect that this is a LuiseStore fork.

Suggested topics:
- `ios`
- `jailbreak`
- `trollstore`
- `luisestore`
- `coretrust`
- `ipa-installer`
